import 'package:flutter/material.dart';

class FacoritesRecipes extends StatelessWidget {
  const FacoritesRecipes({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Favorites Recipes Screen'),
    );
  }
}
